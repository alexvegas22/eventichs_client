package dti.g55.eventich_client.presentation.presentateur

import org.junit.Test

class MeteoPresentateurTest {
    @Test
    fun `Étant donné un présentateur nouvellement instancié, lorsqu'on appelle la fonction init, on obtient une objet appelle la fonction setupListeHeures dans une vue`() {

    }

    @Test
    fun `Étant donné un présentateur nouvellement instancié, lorsqu'on appelle la fonction traiterClickBoutonRetour, on obtient un appel de la fonction retour de la vue`() {

    }
}